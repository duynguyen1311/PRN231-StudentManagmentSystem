@page
@using BusinessObject.Utility;
@model StudentManagingSystem_Client.Pages.DepartmentPage.DepartmentModel
@{
}

<style>
    .checkbox-wrapper-26 * {
        -webkit-tap-highlight-color: transparent;
        outline: none;
    }

    .checkbox-wrapper-26 input[type="checkbox"] {
        display: none;
    }

    .checkbox-wrapper-26 label {
        --size: 50px;
        --shadow: calc(var(--size) * .07) calc(var(--size) * .1);
        position: relative;
        display: block;
        width: var(--size);
        height: var(--size);
        margin: 0 auto;
        background-color: #f72414;
        border-radius: 50%;
        box-shadow: 0 var(--shadow) #ffbeb8;
        cursor: pointer;
        transition: 0.2s ease transform, 0.2s ease background-color, 0.2s ease box-shadow;
        overflow: hidden;
        z-index: 1;
    }

        .checkbox-wrapper-26 label:before {
            content: "";
            position: absolute;
            top: 50%;
            right: 0;
            left: 0;
            width: calc(var(--size) * .7);
            height: calc(var(--size) * .7);
            margin: 0 auto;
            background-color: #fff;
            transform: translateY(-50%);
            border-radius: 50%;
            box-shadow: inset 0 var(--shadow) #ffbeb8;
            transition: 0.2s ease width, 0.2s ease height;
        }

        .checkbox-wrapper-26 label:hover:before {
            width: calc(var(--size) * .55);
            height: calc(var(--size) * .55);
            box-shadow: inset 0 var(--shadow) #ff9d96;
        }

        .checkbox-wrapper-26 label:active {
            transform: scale(0.9);
        }

    .checkbox-wrapper-26 .tick_mark {
        position: absolute;
        top: -1px;
        right: 0;
        left: calc(var(--size) * -.05);
        width: calc(var(--size) * .6);
        height: calc(var(--size) * .6);
        margin: 0 auto;
        margin-left: calc(var(--size) * .14);
        transform: rotateZ(-40deg);
    }

        .checkbox-wrapper-26 .tick_mark:before,
        .checkbox-wrapper-26 .tick_mark:after {
            content: "";
            position: absolute;
            background-color: #fff;
            border-radius: 2px;
            opacity: 0;
            transition: 0.2s ease transform, 0.2s ease opacity;
        }

        .checkbox-wrapper-26 .tick_mark:before {
            left: 0;
            bottom: 0;
            width: calc(var(--size) * .1);
            height: calc(var(--size) * .3);
            box-shadow: -2px 0 5px rgba(0, 0, 0, 0.23);
            transform: translateY(calc(var(--size) * -.68));
        }

        .checkbox-wrapper-26 .tick_mark:after {
            left: 0;
            bottom: 0;
            width: 100%;
            height: calc(var(--size) * .1);
            box-shadow: 0 3px 5px rgba(0, 0, 0, 0.23);
            transform: translateX(calc(var(--size) * .78));
        }

    .checkbox-wrapper-26 input[type="checkbox"]:checked + label {
        background-color: #07d410;
        box-shadow: 0 var(--shadow) #92ff97;
    }

        .checkbox-wrapper-26 input[type="checkbox"]:checked + label:before {
            width: 0;
            height: 0;
        }

        .checkbox-wrapper-26 input[type="checkbox"]:checked + label .tick_mark:before,
        .checkbox-wrapper-26 input[type="checkbox"]:checked + label .tick_mark:after {
            transform: translate(0);
            opacity: 1;
        }
</style>


<div class="mobile-menu-overlay"></div>

<div class="main-container">
    <div class="pd-ltr-20 xs-pd-20-10">
        <div class="min-height-200px">
            <div class="page-header">
                <div class="row">
                    <div class="col-md-6 col-sm-12">
                        <div class="title">
                            <h4>Department List</h4>
                        </div>
                        <nav aria-label="breadcrumb" role="navigation">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a asp-page="Index">Home</a></li>
                                <li class="breadcrumb-item active" aria-current="page">Department</li>
                            </ol>
                        </nav>
                    </div>
                    @if (User.IsInRole(RoleConstant.ADMIN))
                    {
                        <div class="col-md-3 col-sm-12 text-right">
                            <div class="dropdown">
                                <a class="btn btn-primary" asp-page="AddDepartment">
                                    Add Department
                                </a>
                            </div>
                        </div>
                    }
                    @if (User.IsInRole(RoleConstant.ADMIN))
                    {
                        <div class="col-md-1 col-sm-12 text-right">
                            <div class="dropdown">
                                <a id="deleteButton" class="btn btn-danger disabled" asp-page="DeleteDepartment">
                                    Delete
                                </a>
                            </div>
                        </div>
                    }
                    @if (User.IsInRole(RoleConstant.ADMIN))
                    {
                        <div class="col-md-1 col-sm-12 text-left">
                            <div class="dropdown">
                                <a class="btn btn-outline-danger" asp-page="ExportDepartment">
                                    Export
                                </a>
                            </div>
                        </div>
                    }
                    @if (User.IsInRole(RoleConstant.ADMIN))
                    {
                        <div class="col-md-1 col-sm-12 text-left">
                            <div class="dropdown">
                                <a class="btn btn-outline-success" onclick="chooseFile()">
                                    Import
                                </a>
                                <form id="uploadForm" method="post" enctype="multipart/form-data" asp-page="ImportDepartment" style="display: none;">
                                    <input id="fileInput" type="file" name="file" accept=".xlsx, .xls" />
                                    <button id="submitBtn" type="submit">Submit</button>
                                </form>
                            </div>
                        </div>
                    }
                </div>
            </div>
            <form method="get">
                <div class="row mb-15">
                    <div class="col-md-4">
                        <div class="header-search">
                            <form method="get">
                                <div class="form-group mb-0" style="display:flex">
                                    <i style="justify-content: flex-start;"
                                       class="dw dw-search2 search-icon mr-10 mt-15"></i>
                                    <input asp-for="Keyword" style="justify-content: flex-end;" type="text"
                                           class="form-control search-input" placeholder="Search Here">
                                </div>
                            </form>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div style="display: flex;">
                            <p style="justify-content: flex-start; margin-top:9px" class="mr-10">Status</p>
                            <div style="justify-content: flex-end; width: 200px;">
                                <select asp-for="Status" class="custom-select">
                                    <option selected="" value="null">All</option>
                                    <option value="True">Hoạt động</option>
                                    <option value="False">Không hoạt động</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    <button type="submit" style="height: 44px;margin-left: 20px;width: 114px;" class="btn btn-success">Filter</button>
                    @*<div class="col-md-3">
                    <div style="display: flex;">
                    <p style="justify-content: flex-start;margin-top:9px" class="mr-10">Status</p>
                    <div style="justify-content: flex-end; width: 200px;">
                    <select class="custom-select">
                    <option selected="">All</option>
                    <option value="1">One</option>
                    <option value="2">Two</option>
                    <option value="3">Three</option>
                    </select>
                    </div>
                    </div>

                    </div>*@
                </div>
            </form>
            <div class="table-responsive">
                <table class="table table-striped table-bordered ">
                    <thead style="background-color: lightgray">
                        <tr>
                            <th>#</th>
                            <th>Department Name</th>
                            <th>Department Code</th>
                            <th>Status</th>
                            <th>Created Date</th>
                            <th>Last Modified Date</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.ListDepartment.Data)
                        {
                            <tr>
                                <td>
                                    <input type="checkbox" id="_checkbox-26" name="selectedItems" value="@item.Id">
                                </td>
                                <td class="table-plus">@item.DepartmentName</td>
                                <td>@item.DepartmentCode</td>
                                <td>@(item.Status == true ? "Hoạt động" : "Không hoạt động")</td>
                                <td>@(item.CreatedDate != null ? item.CreatedDate.Value.ToShortDateString() : "")</td>
                                <td>@(item.LastModifiedDate != null ? item.LastModifiedDate.Value.ToShortDateString() : "")</td>
                                <td>
                                    <div class="dropdown">
                                        <a class="btn btn-link font-24 p-0 line-height-1 no-arrow dropdown-toggle" href="#" role="button" data-toggle="dropdown">
                                            <i class="dw dw-more"></i>
                                        </a>
                                        @if (User.IsInRole(RoleConstant.ADMIN))
                                        {
                                            <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                                                <a class="dropdown-item" asp-page="UpdateDepartment" asp-route-id="@item.Id" asp-route-pageIndex="@Model.PageIndex"><i class="dw dw-edit2"></i> Edit</a>
                                                <a class="dropdown-item" asp-page="DeleteDepartment" asp-route-id="@item.Id" onclick="return confirmDelete(event)"><i class="dw dw-delete-3"></i> Delete</a>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
                                                <a class="dropdown-item" asp-page="UpdateDepartment" asp-route-id="@item.Id"><i class="dw dw-edit2"></i> View</a>
                                            </div>
                                        }

                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            @{
                var prev = Model.PageIndex > 1 ? "" : "disabled";
                var next = Model.PageIndex <= Model.TotalPage ? "" : "disabled";
            }
            <div class="row">
                <div class="btn-group mb-15" style="margin-left:15px;">
                    <a class="btn btn-light @prev" asp-page="./Department" asp-route-pageIndex="@(Model.PageIndex-1)">Previous</a>
                    @for (int i = 1; i <= Model.TotalPage; i++)
                    {
                        <a class="btn @(i == Model.PageIndex ? "btn-success" : "btn-light")" asp-page="./Department" asp-route-pageIndex="@i">@i</a>
                    }
                    <a class="btn btn-light @next" asp-page="./Department" asp-route-pageIndex="@(Model.PageIndex+1)">Next</a>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    function confirmDelete(event) {
        event.preventDefault(); // prevent the default action of the button click
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                // if the user clicks the confirm button, continue with the delete action
                window.location.href = event.target.href;
            }
        });
        return false;
    }
</script>

<script>
    function chooseFile() {
        var fileInput = document.getElementById('fileInput');
        fileInput.click();
    }

    document.getElementById('fileInput').addEventListener('change', function () {
        var submitBtn = document.getElementById('submitBtn');
        submitBtn.click();
    });
</script>
<script>
    var checkboxes = document.querySelectorAll('input[name="selectedItems"]');
    var deleteButton = document.getElementById('deleteButton');

    function handleCheckboxChange() {
        var checkedCount = 0;
        checkboxes.forEach(function (checkbox) {
            if (checkbox.checked) {
                checkedCount++;
            }
        });

        if (checkedCount >= 2) {
            deleteButton.classList.remove('disabled');
        } else {
            deleteButton.classList.add('disabled');
        }
    }

    checkboxes.forEach(function (checkbox) {
        checkbox.addEventListener('change', handleCheckboxChange);
    });
</script>